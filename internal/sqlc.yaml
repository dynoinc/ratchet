version: "2"
sql:
  - engine: "postgresql"
    queries: "schema/query.sql"
    schema: "schema/migrations"
    gen:
      go:
        package: "schema"
        out: "schema"
        sql_package: "pgx/v5"
